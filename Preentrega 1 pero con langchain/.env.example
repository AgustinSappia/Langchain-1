# Copiá este archivo a ".env" y completá con tus claves reales.
# NUNCA subas el .env real a GitHub (agregalo a .gitignore).

OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...

# Proveedor por defecto que usará main.py: "openai" o "anthropic"
LLM_PROVIDER=openai
